# Use the live Supplier backend (default): backend
# Use browser-only seeded data instead: mock
VITE_SUPPLIER_API_MODE=backend

# Optional absolute API URL. Leave unset to use the Host's /api/supplier proxy.
# VITE_SUPPLIER_API_URL=http://localhost:3000/api/supplier
